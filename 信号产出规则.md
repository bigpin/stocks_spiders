# 股票信号产出规则

## 概述

系统基于技术指标分析，自动检测股票买入信号。信号检测分为两个阶段：
1. **历史信号统计**：遍历历史数据，统计各信号类型的成功率和出现次数
2. **最近3天信号筛选**：从最近3个交易日中筛选高胜率信号

## 前置条件

- **数据要求**：至少需要 60 天的历史K线数据（保证 MA60、均量等统计更稳定）
- **输出条件**：只有当最近3天出现**6种以上不同信号类型**时，才会输出报告

## 信号类型及检测规则

### 1. KDJ 信号

#### KDJ超卖 (`kdj_oversold`)
- **条件**：`K_9_3 < 20` 且 `D_9_3 < 20`
- **含义**：KDJ指标进入超卖区域，可能反弹

#### KDJ金叉 (`kdj_golden_cross`)
- **条件**：前一日 `K < D`，当日 `K > D`（K线上穿D线）
- **含义**：短期动能转强

#### KDJ底背离 (`kdj_divergence`)
- **条件**：价格创新低，但K值未创新低
- **含义**：下跌动能减弱，可能反转

### 2. MACD 信号

#### MACD金叉 (`macd_golden_cross`)
- **条件**：前一日 `MACD < MACD信号线`，当日 `MACD > MACD信号线`
- **含义**：趋势转强信号

#### MACD零轴上穿 (`macd_zero_cross`)
- **条件**：前一日 `MACD < 0`，当日 `MACD > 0`
- **含义**：从负值转为正值，动能转强

#### MACD底背离 (`macd_divergence`)
- **条件**：价格创新低，但MACD未创新低
- **含义**：下跌动能减弱

### 3. RSI 信号

#### RSI超卖 (`rsi_oversold`)
- **条件**：`RSI_6 < 20`
- **含义**：相对强弱指标超卖，可能反弹

#### RSI金叉 (`rsi_golden_cross`)
- **条件**：前一日 `RSI_6 < RSI_12`，当日 `RSI_6 > RSI_12`
- **含义**：短期RSI上穿长期RSI，动能转强

### 4. BOLL（布林带）信号

#### BOLL下轨支撑 (`boll_bottom_touch`)
- **条件**：收盘价 `<= 下轨 * 1.01`（接近或触及下轨）
- **含义**：价格触及下轨，可能反弹

#### BOLL带宽扩张 (`boll_width_expand`)
- **条件**：当日带宽 `> 前一日带宽 * 1.1`（带宽扩张10%以上）
- **含义**：波动性增加，可能有大行情

### 5. MA（移动平均线）信号

#### MA5上穿MA20 (`ma_golden_cross`)
- **条件**：前一日 `MA5 < MA20`，当日 `MA5 > MA20`
- **含义**：短期均线上穿长期均线，趋势转强

#### MA20支撑 (`ma_support`)
- **条件**：收盘价在 `MA20 * 0.99` 到 `MA20 * 1.01` 之间（接近MA20）
- **含义**：价格在均线支撑位附近，可能反弹

### 6. DMI 信号

#### DMI金叉 (`dmi_golden_cross`)
- **条件**：前一日 `DI+ < DI-`，当日 `DI+ > DI-`，且 `ADX > 20`
- **含义**：上升趋势转强，且趋势强度足够

#### ADX强势 (`dmi_adx_strong`)
- **条件**：`ADX > 30`
- **含义**：趋势强度高，趋势明确

### 7. CCI 信号

#### CCI超卖 (`cci_oversold`)
- **条件**：`CCI_20 < -100`
- **含义**：商品通道指标超卖，可能反弹

#### CCI零轴上穿 (`cci_zero_cross`)
- **条件**：前一日 `CCI < 0`，当日 `CCI > 0`
- **含义**：从负值转为正值，动能转强

### 8. ROC 信号

#### ROC零轴上穿 (`roc_zero_cross`)
- **条件**：前一日 `ROC_12 < 0`，当日 `ROC_12 > 0`
- **含义**：变动率从负转正，动能转强

#### ROC底背离 (`roc_divergence`)
- **条件**：价格创新低，但ROC未创新低
- **含义**：下跌动能减弱

## 成功率计算规则

### 成功定义
- **标准**：信号出现后，未来14天内最高涨幅达到 **5%** 以上
- **计算方式**：`max_return = (未来14天最高价 - 信号日收盘价) / 信号日收盘价 * 100`
- **窗口不足处理**：若未来不足14天，不计入成功率统计（避免系统性压低胜率）

### 统计指标
- **信号胜率**：该信号类型历史成功率 = `成功次数 / 总出现次数 * 100`
- **整体胜率**：所有信号类型的综合成功率 = `总成功次数 / 总信号数 * 100`

## 最近3天信号筛选规则

### 筛选条件（必须同时满足）
1. **历史样本充足**：该信号类型历史出现次数 `> 8` 次
2. **信号胜率达标**：该信号类型历史成功率 `>= 60%`
3. **整体胜率达标**：所有信号类型的整体成功率 `>= 50%`
4. **流动性达标**：近20日平均成交额 >= 1亿，近20日平均换手率 >= 1%，当日成交量 / 20日均量 >= 0.7（趋势类信号需 >= 1.2）
5. **估值过滤**：PE 在 0–80，PB 在 0–8；若估值数据缺失则跳过过滤

### 量能热度分（0–100，仅展示、不参与过滤）

在通过上述条件并写入报告时，对标的计算「最近交易热度」总分，用于同一批次内比较**资金关注度与放量程度**（不改变是否入选，仅排序参考）：

| 分项 | 权重 | 含义 |
|------|------|------|
| 量趋势 | 40 分 | 近 5 日均量 / 近 20 日均量（r_trend）。默认用**过去约 120 个交易日**内该比值的**历史分位数**映射到 0–40（跨股票自适应）；历史样本不足时退回固定区间线性映射。 |
| 近几日放量 | 25 分 | **近 3 日均量 / MA20**（非单日），减轻「一天拉爆」噪声；并与 r_trend 做**依赖约束**：`得分 = 线性分 × min(1, r_trend)`，避免趋势未抬升却单日放量被过度奖励。 |
| 成交活跃 | 35 分 | 近 5 日均成交额 / 近 20 日均成交额（资金关注度，高价股更友好）；无成交额列时用「近 5 日均量 / 再前一段均量」近似。 |

**流动性折扣**：分项求和后（封顶 100）再乘以 `min(1, 近20日均成交额 / 1e8)`，避免冷门票被异常放量推高。

每只入选股票在报告中**仅输出一行**，紧跟在「总成功数」之后，例如：`最近交易热度评分: 78/100`。

配置项见 `stock_config.py` 中 `SIGNAL_FILTERS['volume_heat']`（可关闭 `enable: False`，可调 `weights`、`percentile_lookback`、`liquidity_floor` 等）。

### 时间范围
- **检测窗口**：最近3个交易日（排除周末）
- **日期匹配**：最后一个交易日必须与报告日期一致

## 输出规则

### 报告生成条件
1. 最近3天至少出现 **6种以上不同信号类型**
2. 满足筛选条件的信号才会被输出

### 报告内容
- **汇总信息**：
  - 总体成功率
  - 总信号数（历史所有信号）
  - 总成功数
- **最近信号详情**：
  - 信号数量统计（按类型分组）
  - 每个信号的详细信息（日期、类型、胜率、指标值等）

## 技术指标参数配置

| 指标 | 参数 | 说明 |
|------|------|------|
| KDJ | period=9, signal=3 | 9日周期，3日平滑 |
| MACD | fast=12, slow=26, signal=9 | 快线12，慢线26，信号线9 |
| RSI | periods=[6, 12, 24] | 6日、12日、24日 |
| BOLL | period=20, std=2 | 20日周期，2倍标准差 |
| MA | periods=[5, 10, 20, 30, 60] | 5日、10日、20日、30日、60日均线 |
| DMI | length=14, signal=14 | 14日周期 |
| CCI | length=20 | 20日周期 |
| ROC | length=12 | 12日周期 |

## 示例：sh601231 (2026-01-15)

根据报告文件，该股票在最近3天检测到：
- **9个信号事件**，**6种信号类型**：
  1. KDJ超卖：2个（2026-01-13, 2026-01-14）
  2. ADX强势：3个（2026-01-13, 2026-01-14, 2026-01-15）
  3. KDJ金叉：1个（2026-01-14）
  4. RSI金叉：1个（2026-01-15）
  5. CCI零轴上穿：1个（2026-01-15）
  6. ROC零轴上穿：1个（2026-01-15）

- **整体胜率**：66.20%
- **总信号数**：361（历史累计）
- **总成功数**：239

## 注意事项

1. **数据质量**：信号检测依赖技术指标计算的准确性，需要足够的历史数据
2. **市场环境**：信号成功率受市场环境影响，不同市场阶段表现可能不同
3. **风险提示**：技术指标信号仅供参考，不构成投资建议
4. **时间窗口**：成功判断基于未来14天，实际持有时间可能不同

## 中线（1–3个月）扩展评估规则（建议新增）

在保留上文 14 日短线成功率统计的基础上，建议增加面向中线持仓的评估维度，用于服务 1–3 个月波段交易：

### 中线窗口与指标

- **中线评估窗口**：信号出现后的未来 60 个交易日  
- **新增统计指标**：
  - 中线最大涨幅：`max_return_60`
  - 中线最终收益：`final_return_60`（第60个交易日或提前终止时的收益）
  - 中线最大回撤：`max_drawdown_60`
  - 收益/回撤比：`rr_ratio_60 = max_return_60 / max(1, |max_drawdown_60|)`

### 中线成功定义（建议）

- **中线成功**：可按以下任一条件定义为成功（具体阈值可回测调参）：
  - 期内最大涨幅 ≥ 15% 且最大回撤 ≤ 10%；或
  - 最终收益 ≥ 8% 且最大回撤 ≤ 12%。
- **中线胜率**：在 60 日窗口下满足“中线成功”条件的比例。

### 信号保留标准（中线版）

在原有 14 日成功率规则基础上，增加中线过滤：

1. 仍然要求：  
   - 历史出现次数 `> 8` 次；  
   - 14 日短线成功率 `>= 60%`；  
   - 整体短线成功率 `>= 50%`。
2. 额外要求（用于中线交易池）：  
   - `rr_ratio_60` 的历史中位数 `> 1.5`；  
   - 中线胜率 `>= 55%`（或结合回测结果调参）。

满足上述中线条件的信号，可划入“中线信号池”，用于偏 1–3 个月的波段策略。

## 多层过滤与技术评分（建议新增）

在现有“最近3天出现 ≥6 种不同信号类型”规则之上，建议引入多层过滤与加权评分：

### 多层过滤结构

1. **基础白名单（按季度/半年更新）**  
   - 依据市值、ROE、营收增速、负债率、日均成交额等基本面与流动性指标，筛出“基础体质尚可”的股票池。  
   - 仅对白名单内股票计算并输出技术信号，避免垃圾股占用资源。

2. **技术信号过滤（沿用本规则）**  
   - 在白名单股票池上运行 KDJ/MACD/RSI/BOLL/MA/DMI/CCI/ROC 等信号检测与历史统计。  

3. **走势结构过滤（建议结合K线形态/均线位置）**  
   - 剔除：价格远离长期均线（如偏离 60 日均线 > 30%）且放量冲高的高位结构；  
   - 优先：  
     - 中长期上升趋势中的回踩 + 多指标共振；  
     - 长期底部区域刚启动、放量突破关键均线的个股。

### 技术评分（示意）

为不同信号设置权重，形成 0–100 分的技术评分，供排序与筛选使用：

- **趋势类信号（权重较高）**  
  - MA5 上穿 MA20（`ma_golden_cross`）、MACD 金叉（`macd_golden_cross`）、DMI 金叉/ADX 强势（`dmi_golden_cross` / `dmi_adx_strong`）；
- **震荡类信号（权重中等）**  
  - KDJ 超卖/金叉（`kdj_oversold` / `kdj_golden_cross`）、RSI 超卖/金叉（`rsi_oversold` / `rsi_golden_cross`）、CCI 超卖/零轴上穿等；
- **波动/放大类信号（权重辅助）**  
  - BOLL 带宽扩张（`boll_width_expand`）等，多与趋势/震荡信号共振时加分。

示例（可在代码中实现具体公式）：

- 每种信号类型设定基础分值，如：趋势类 8–10 分，震荡类 5–7 分，波动类 3–5 分；  
- 对最近 N 日（如 3–5 日）出现的信号按权重累加；  
- 对出现于中线高质量历史统计区间的信号，额外给予系数加成；  
- 将总分截断/归一化到 0–100 区间，作为技术评分。

报告中可新增输出：

- 每只股票的：技术评分、中线胜率、60 日收益/回撤统计；  
- 结合基本面评分、流动性达标情况，给出“偏短线/偏中线”的标签与参考仓位建议。
