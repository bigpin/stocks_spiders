# 从报告到「数据洞察」：把技术信号装进小程序与 Web

> 本文稿可直接复制到公众号编辑器；文中 `TODO-image-*.png` 为占位图，发布前请替换为真实截图。

---

## 开篇：为什么做这件事

做股票信号分析时，我们长期依赖 K 线爬取、多指标计算和每日文本报告。报告里信息很全，但「翻文件、对日期、人肉筛」成本高，也不利于日常复盘。于是我们把同一套数据接进两条产品形态：**微信小程序「数据洞察」**（随身看、可订阅推送）和 **配套 Web 视图**（大屏、多条件筛选、止盈止损触达率矩阵）。本文说明两者各自解决什么问题，以及数据从哪来；**不涉及安装、部署与命令行步骤**，你只需知道：**小程序扫码即可用，Web 在电脑浏览器打开配套页面即可用。**

**应放内容：** 封面主视觉（建议：小程序 + Web 同屏，或「数据洞察」导航栏特写）。

![配图：封面主视觉](TODO-image-1.png)

---

## 一图看懂「数据洞察」

「数据洞察」在小程序导航栏中的名称即「数据洞察」，定位可以理解为日常看盘的**信号备忘录**：云端写入当日汇总与明细后，你在手机上按日期、按股票浏览；需要时订阅消息，减少漏看。

数据流简述：

- **输入**：腾讯云开发（CloudBase）数据库集合 `stock_signals`，其中 `doc_type=stock_summary` 为按日、按股的汇总头，`doc_type=signal_event` 为具体信号明细（通过 `report_id` 关联）。
- **输出**：按日期分组的列表卡片；可选的**订阅消息**（页面内提供「订阅 / 取消订阅」及授权提示）。

**小程序功能要点（一张图尽量带全）：** 顶部「数据洞察订阅」与订阅提示；最近报告日默认展开，下方「其他日期」可先只看摘要、展开后再加载；股票卡片上为**整体胜率、特征数、交易热度**（有数据时带标签色）；展开后为**特征类型统计**与每条信号的**中文名、准确率、历史出现、收盘价、信号日期**；点股票代码可选东方财富、新浪财经或复制代码（外链多为复制链接到剪贴板后在浏览器打开）；底部「分析规则说明」可复制公众号规则文链接。

**应放内容：** 一张竖屏长图或拼图：首页（订阅条 + 日期分组 + 导航栏「数据洞察」）+ 任选一张展开后的卡片局部，便于读者一眼看懂信息流。

![配图：小程序首页与卡片展开（可拼图）](TODO-image-2.png)

---

## 配套 Web 视图：信号的「驾驶舱」

Web 与小程序共用同源思路（信号、胜率、热度等字段），面向**桌面大屏与深度筛选**：主界面为 **时间轴视图、收益矩阵、数据列表** 三个标签，左侧筛选项随标签切换。

**时间轴**：垂直时间轴展示**买入点、最高价线、最低价线**等；买入点颜色与「买入当日 / 次日涨跌」组合相关；**Ctrl + 滚轮**缩放时间密度，普通滚轮滚动；可点击高亮同股。侧栏支持股票代码、日期、买入价区间、买卖日涨跌（可选滑块）、交易热度等筛选。页面上方有**两行统计**：**全库概览**（总信号、股票数、平均成功率、平均最高涨幅）与**当前筛选后**（实际进入时间轴的信号数、涉及股票、平均成功率、平均最高/最低涨幅），便于对比全库与当前筛选结果。

**收益矩阵**：二维网格覆盖多组止盈（约 2%～30%，步长 2%）与止损（约 -2%～-30%）。每格给出**触达率**、触发中止盈/止损占比、触发均值等；可选「**未触达时强制持有后卖出**」及持有天数。侧栏可设**触达率筛选阈值**：**未达标的格子置灰**，且**不参与 Top10 排名**；悬停单元格可看汇总与部分个股明细。

**数据列表**：表格分页、表头排序、多条件搜索，适合批量核对与检索（无需单独配图时可在上一张 Web 截图里带一角标签切换示意即可）。

**应放内容：** 一张横屏 Web 截图，优先包含「三标签 + 时间轴主区 + 双行统计」；若版面允许可与侧栏同框。

![配图：Web 时间轴与双行统计（含标签与侧栏更佳）](TODO-image-3.png)

**应放内容：** 一张收益矩阵整表（若悬停 tooltip 清晰可二选一，不必再单独一张）。

![配图：收益矩阵整表](TODO-image-4.png)

---

## 信号是怎么来的（精简版）

后端在历史 K 线上检测多类技术信号并做回测统计。成功判定在实现上为：信号日后 **`success_window_days`（默认 14）个交易日**内，以**收盘价序列**计算的最大涨幅是否达到 **5%**；窗口不足完整天数的样本不参与该类型胜率统计。写出报告前还会综合**最近若干交易日不同信号种类数**、**单类型历史样本量与胜率**、**整体胜率**、**流动性**、**估值**等条件；具体阈值以配置为准。更完整的文字说明见仓库内《信号产出规则》及文末延伸阅读。

---

## 如何使用（结果导向）

- **小程序**：用微信进入「数据洞察」即可浏览；需要推送时点「订阅」，并按页面提示完成授权（建议在弹窗中勾选「总是保持以上选择」以便长期接收）。
- **配套 Web**：在电脑浏览器打开配套页面即可使用三标签视图、矩阵与列表；与小程序同源逻辑，侧重大屏筛选与触达率分析。

**应放内容：** 正式版小程序码（用于文末引导关注/进入）。

![配图：小程序码](TODO-image-5.png)

---

## 风险提示与说明

本文及「数据洞察」小程序、配套 Web 所展示的数据、胜率、触达率、矩阵着色等，均基于**历史数据与既定规则**计算，**仅供学习、研究与复盘参考，不构成任何投资建议**。证券市场存在波动与不确定性；历史统计不代表未来表现。请根据自身情况独立判断，审慎决策。

---

## 延伸阅读与仓库链接

- 项目总览：[README.md](README.md)
- 信号产出与过滤规则（随代码迭代更新）：[信号产出规则.md](信号产出规则.md)
- Web 功能与接口说明：[Spiders/web/README.md](Spiders/web/README.md)
- 小程序内「分析规则说明」所复制的历史公众号文章（规则向长文）：<https://mp.weixin.qq.com/s/vZ4Mg1Ll8_OX1Mgl1VCddA>

---

## 贴图清单（共 5 张，发布前替换）

| 占位文件 | 建议替换内容 |
|----------|----------------|
| TODO-image-1.png | 封面主视觉 |
| TODO-image-2.png | 小程序：首页 + 展开卡片（可拼图） |
| TODO-image-3.png | Web：时间轴 + 双行统计 + 标签与侧栏（尽量一屏） |
| TODO-image-4.png | Web：收益矩阵整表 |
| TODO-image-5.png | 小程序码 |

统一导出宽度（例如 900px）有利于公众号排版；发布后可将占位路径改为图床 URL，或仅保留图注。
